import { PlanoClient } from './PlanoClient'

export const metadata = {
  title: 'Plano Diario | Kalam',
  description: '3 micro-tarefas espirituais para cada dia. Leitura, pratica e reflexao.',
}

export default function PlanoDiarioPage() {
  return <PlanoClient />
}
